

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - VITE_EMBEDDING_SERVER_URL=http://embedding:8000
    env_file:
      - .env
    dns:
      - 8.8.8.8
    depends_on:
      - embedding
    volumes:
      - ./sqlite.db:/app/sqlite.db

  embedding:
    build:
      context: .
      dockerfile: Dockerfile.python
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./sqlite.db:/app/sqlite.db
      - ./embedding_cache.db:/app/embedding_cache.db
      - ./rag_cache.db:/app/rag_cache.db
